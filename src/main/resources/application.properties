spring.application.name=backend1

# ?????????????????????????????? Render
server.port=8082
server.address=0.0.0.0

# ---------- DataSource ?????? ----------
# ??? DB_URL / DB_USER / DB_PASS ????????? ENV (?????? Render)
# ? Spring ????? PostgreSQL ?? Render
# ???????? ENV ??? (???????????????????)
# ? ?? fallback ????? MySQL localhost ????????????

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/backendutcc}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:}

# ---------- Hibernate ----------
spring.jpa.hibernate.ddl-auto=update
# ???????? dialect ? Hibernate auto-detect (?????????? MySQL ??? PostgreSQL)

# ---------- Health ----------
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
