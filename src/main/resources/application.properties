# --- Application Info ---
spring.application.name=backend1

# --- Server ---
server.port=8082

# --- Database Connection ---
spring.datasource.url=jdbc:mysql://localhost:3306/backendutcc?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=utcc2025

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- CORS (??????????????? frontend) ---
app.cors.allowed-origins=http://localhost:5173

# --- Optional: Logging ---
logging.level.org.springframework.web=INFO
logging.level.com.example.backend1=DEBUG
